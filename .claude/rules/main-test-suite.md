---
paths:
  - "/Jint.Tests/**"
---

You are editing the main test suite. `Jint.Tests/AGENTS.md` carries the two assembly attributes that make an NUnit run mean what the xUnit run meant — and that are compiled into four assemblies, not one — the rule that a wall-clock number is either the assertion or a wedge ceiling and never both, the helpers that replace xUnit's vocabulary, per-file nullable, and the four tests here that hold the repository to its own rules and are never satisfied by editing what they check.

**Read [`Jint.Tests/AGENTS.md`](../../Jint.Tests/AGENTS.md) before you edit, and [`Jint.Tests/Wpt/AGENTS.md`](../../Jint.Tests/Wpt/AGENTS.md) when what you are editing is the web-platform-tests corpus or its driver.** Neither is repeated here or in the repository-root
`AGENTS.md`; that file's index says what each co-located instruction file covers.
