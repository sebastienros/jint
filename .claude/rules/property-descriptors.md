---
paths:
  - "Jint/Runtime/Descriptors/**"
---

You are editing a property descriptor. `Jint/Runtime/Descriptors/AGENTS.md` carries the descriptor's own rows of what counts as a public contract, why `PropertyFlag.CustomJsValue` is the supported lazy-value hook, and why a value that is lazy only *once* belongs on `PropertyDescriptor.CreateLazy` instead.

**Read [`Jint/Runtime/Descriptors/AGENTS.md`](../../Jint/Runtime/Descriptors/AGENTS.md) before you edit.** It is not repeated here or in the repository-root
`AGENTS.md`; that file's index says what each co-located instruction file covers.
